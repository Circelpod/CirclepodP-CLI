{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/circlepodp-cli",
            "args": ["create-spl"],
            "cwd": "${workspaceRoot}",
            "sourceLanguages": ["rust"],
            "terminal": "integrated",
            "env": {
                "RUST_LOG": "debug" 
            }
        }
    ]
}